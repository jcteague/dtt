define 'general_view', ['backbone'], (Backbone) ->

    class GeneralView extends Backbone.View
        id: 'view_id'

        events: () ->
            return

        initialize: ->
            return

        render: ->
            return

        listen_to:(param) ->
            return

        append_to: (parent) ->
            @$el.appendTo parent
