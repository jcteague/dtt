// Generated by CoffeeScript 1.3.3
(function() {

  define('form_template_renderer', ['jquery'], function($) {
    var FormTemplateRenderer;
    return FormTemplateRenderer = (function() {

      function FormTemplateRenderer() {}

      FormTemplateRenderer.prototype.render = function(collection) {
        var form, form_templates, generatorSelector, templateFieldBuilder, textFieldBuilder;
        textFieldBuilder = function(template) {
          return [
            $('<label>', {
              "for": template.name
            }), $('<input>', {
              "type": "text",
              "name": template.name
            })
          ];
        };
        templateFieldBuilder = function(template) {};
        generatorSelector = function(fieldType) {
          if (fieldType === 'string') {
            return textFieldBuilder;
          }
        };
        form = $('<form>', {
          action: collection.uri
        });
        form_templates = collection.template.data;
        form_templates.forEach(function(template) {
          var fieldElements, fieldGenerator;
          fieldGenerator = generatorSelector(template.type);
          fieldElements = fieldGenerator(template);
          return fieldElements.forEach(function(f) {
            return form.append(f);
          });
        });
        return form;
      };

      return FormTemplateRenderer;

    })();
  });

}).call(this);
