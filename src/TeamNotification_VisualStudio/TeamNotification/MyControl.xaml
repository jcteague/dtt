<UserControl x:Class="AvenidaSoftware.TeamNotification.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vsfx="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.10.0"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow"
             Background="{DynamicResource {x:Static vsfx:VsBrushes.ToolWindowBackgroundKey}}">
    <Grid>
        <StackPanel Orientation="Vertical">
            <Grid Height="100" Name="grid1" Width="298">
                <ScrollViewer Height="auto" HorizontalAlignment="Stretch" Name="scrollViewer1" VerticalAlignment="Stretch" Width="auto" VerticalScrollBarVisibility="Auto">
                    <StackPanel Height="auto" Name="messageList" Width="auto" Orientation="Vertical" />
                </ScrollViewer>
            </Grid>
            <TextBox Height="24" Name="messageTextBox" Width="226" Text="Write Here" />
            <Button Content="Send" Height="23" Name="sendMessageButton" Width="75" Click="sendMessageButton_Click" />
        </StackPanel>
    </Grid>
</UserControl>
